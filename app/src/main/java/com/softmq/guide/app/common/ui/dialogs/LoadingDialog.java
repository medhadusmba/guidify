package com.softmq.guide.app.common.ui.dialogs;

import android.app.Activity;

public class LoadingDialog extends CustomDialog {
    public LoadingDialog(Activity activity, int layout, boolean fullscreen) {
        super(activity, layout, fullscreen);
    }
}
